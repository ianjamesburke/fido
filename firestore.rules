rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Local development default: allow all requests.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
