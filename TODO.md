# To do
Just a really loose step path for jotting down tasks as they come to me. 


---

Remove the buggys pull to refresh, This also won't be necessary once we enable a pulling type approach, potentially with websockets. 

---


# Dec 4

The F to show the filter post modal on the main feed shortcut doesn't show up. 
So there's no... Yeah, it just says vote up, down, or new, or N for new post, or space for details, and P for profile. But it doesn't show F for filter. So we're gonna add them. 
There's also no shortcuts on the action filter post modal. The shortcuts in the footer, though, are just missing. They're perfectly there on the user profile and the post modal, but when I press, there's no shortcuts in that modal. It seems like the footer is a little too small for them to even show up, at least on the web interface. Maybe we increase the size of the web interface, 2e, just a little bit, and decrease the horizontal width a little bit. Also, for the web interface, let's do an intro message and then like a quick track here and then show the terminal and then put all of the info underneath it. Whereas right now the terminal is below a bunch of header content. Also, remove the animations. The website is running really slow and I just want to make it run lightning fast so that 

---

Simplify Web Flow, have eit apeare on the main page



Add a to-do section to the bottom of the readme. Future: I like future ideas, which is add a convenient flow for requesting a feature that opens an issue template in GitHub. 


The spacebar is getting eaten when pulling up the filter modal, so rather than toggling filters, it opens a post in the background. 


Let's make the reply to post modal a little bit smaller. Right now, it's the same size as the thread — the same vertical size. We can keep the horizontal size perfectly the same, but let's maybe reduce the vertical size by 10%. Heck, let's reduce it by like 30%. 



